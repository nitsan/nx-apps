# my-package1

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build my-package1` to build the library.

## Running unit tests

Run `nx test my-package1` to execute the unit tests via [Vitest](https://vitest.dev/).
